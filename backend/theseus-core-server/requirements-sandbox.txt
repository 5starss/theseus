# Theseus sandbox gate dependencies.
# Keep this smaller than requirements.txt; it is only for generated-tool
# compile/import/structure validation inside the sandbox image.
pydantic>=2.0.0
psutil
nvidia-ml-py
markdownify>=1.2.2
beautifulsoup4>=4.12.0
PyYAML>=6.0
