spring:
  config:
    import: optional:file:.env[.properties]


  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
  kafka:
    bootstrap-servers: ${KAFKA_BROKER} # .env의 변수명에 맞춰 설정